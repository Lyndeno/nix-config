{
  targets = {
    swaylock = {
      useImage = false;
      enable = true;
    };
    firefox.profileNames = ["lsanche"];
    vscode.profileNames = ["default"];
    waybar = {
      font = "sansSerif";
    };

    # this has ifd
    blender.enable = false;
  };
}
