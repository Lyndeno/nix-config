name: Check Flake

on:
  push:

  pull_request:
    branches: [ master ]

  workflow_dispatch:

jobs:
  check-flake:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repo
        uses: actions/checkout@v2
      - name: Check Nix Flake
        uses: cachix/install-nix-action@v17
        run: nix flake check